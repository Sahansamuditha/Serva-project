import React from 'react';
import UnifiedSettings from '../../../components/UnifiedSettings';

export default function NotificationSettings() {
  return <UnifiedSettings initialTab="notifications" />;
}
